{
  "name": "automata_project",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "merge-reports": "mochawesome-merge ./cypress/reports/*.json > ./reportOutput.json",
    "generate-htmlReports": "marge reportOutput.json --reportDir cypress/reports",
    "test": "npx cypress run || npm run posttest",
    "pretest": "del-cli ./cypress/reports/*.json",
    "posttest": "npm run merge-reports && npm run generate-htmlReports"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cypress": "^13.11.0",
    "del-cli": "^5.1.0",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.3.0",
    "mochawesome-report-generator": "^6.2.0"
  }
}
